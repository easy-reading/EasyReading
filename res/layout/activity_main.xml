<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/primary_color"
    android:orientation="vertical" >

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_height"
        android:background="@color/primary_color" />

    <android.support.v4.view.ViewPager
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_alignTop="@+id/tabHost"
        android:paddingTop="@dimen/tab_host_height"
        tools:context="nu.info.zeeshan.rnf.MainActivity" />

    <it.neokree.materialtabs.MaterialTabHost
        android:id="@+id/tabHost"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tab_host_height"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/toolbar"
        app:accentColor="#ffc400"
        app:primaryColor="@color/primary_color" >
    </it.neokree.materialtabs.MaterialTabHost>

</RelativeLayout>